# ai-investigator

Minimal CLI skeleton for story question answering.

## Usage

```bash
python src/main.py --story stories/sample_story.xml --engine naive
```

Engines currently return placeholder responses.
